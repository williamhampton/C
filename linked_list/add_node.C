# this is a function to add a node to a linked list

#include <stdio.h>
#include <stdlib.h>

struct node {
  int data;
  struct node* next;
}
int main(){
  struct node* t->data = 5;
  printf("%d", t->data);
}
